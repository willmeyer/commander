package com.willmeyer.commander;

/**
 * The root of all non-error responses to Commands when run in machine mode.  This will always be 
 * formatted as JSON using Google's GSON package, so construct the object appropriately.  If a 
 * command has no special data to return, it can just return one of these.
 */
public class CommandResponse {	
}
